# Must be the least-privilege `crm_svc` role
# (infra/postgres/migrations/013_crm_rls_and_role.sql), NOT the `metrock`
# bootstrap role — see procurement-service/.env.example for why.
DATABASE_URL="postgresql://crm_svc:crm_svc_dev_password@localhost:5432/metrock_erp?schema=public"
PORT=3005
