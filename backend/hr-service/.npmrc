# file:../../packages/backend-common must be COPIED into node_modules, not
# symlinked (npm's default for local file: deps) — see accounting-service/
# .npmrc for the full MODULE_NOT_FOUND gotcha this avoids.
install-links=true
