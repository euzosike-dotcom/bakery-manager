# Must be the least-privilege `accounting_svc` role
# (infra/postgres/migrations/015_accounting_rls_and_role.sql), NOT the
# `metrock` bootstrap role — see procurement-service/.env.example for why.
DATABASE_URL="postgresql://accounting_svc:accounting_svc_dev_password@localhost:5432/metrock_erp?schema=public"
KAFKA_BROKERS="localhost:9092"
# Independent consumer group from ledger-service's — both read erp.events,
# each maintains its own offsets, neither knows the other exists.
KAFKA_CONSUMER_GROUP="accounting-service"
GOVERNANCE_BASE_URL="http://localhost:3008"
# Phase 2 of the Keycloak auth retrofit (see docs/RUNBOOK.md) — replaces
# TenantContextMiddleware entirely; accounting-service has no mobile
# dependency (no SyncModule, confirmed against apps/mobile's api_client.dart),
# so unlike most Phase 2 services this one needs no route exclusions.
KEYCLOAK_ISSUER="http://localhost:8080/realms/metrock"
PORT=3004

# Comma-separated browser origins to allow via CORS (e.g. the future Web
# Console client, docs/SDD.md's client-layer diagram). Unset by default —
# nothing today calls this API from a browser (only the native Flutter
# app + curl, neither subject to CORS), so leaving this empty changes
# nothing. Never a bare wildcard when set; see @metrock/backend-common's
# applySecurityMiddleware for the full reasoning.
# CORS_ALLOWED_ORIGINS="https://app.metrock.example"
